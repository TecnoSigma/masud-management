  <%= render '/partials/header' %>
  <h1><%= t('.title', order_number: @mission.escort_service.order_number) %></h1>
  <hr>
    <strong><%= t('.job_day') %></strong>
  <span><%= @mission.escort_service.job_day %></span>
  <br>
  <strong><%= t('.start_horary') %></strong>
  <span><%= @mission.escort_service.job_horary %></span>
  <br>
  <hr>
  <!-- CUSTOMER DATA -->
  <h2><%= t('.way') %></h2>
  <h4 style='text-decoration: underline'><%= t('.source') %></h4>
  <strong><%= t('.address') %></strong>
  <span><%= full_address(@mission.escort_service, :source) %></span>
  <br><br>
  <h4 style='text-decoration: underline'><%= t('.destiny') %></h4>
  <strong><%= t('.address') %></strong>
  <span><%= full_address(@mission.escort_service, :destiny) %></span>
  <br><br><br>
  <strong><%= t('.observation') %></strong>
  <span><%= @mission.escort_service.observation %></span>
  <br>

  <hr>

  <!-- TEAM DATA-->
  <h2><%= t('.team') %></h2>
  <strong><%= t('.team_name') %></strong>
  <span><%= @mission.team.name %></span>
  <br>
  <strong><%= t('.agents', count: @mission.team.agents.count) %></strong>
  <span><%= MissionsPresenter.fullnames_with_document(@mission.team.agents) %></span>
  <br>

  <hr>

  <!-- GUNS DATA-->
  <h2><%= t('.arsenal') %></h2>
  <strong><%= t('.guns') %></strong>
  <span><%= MissionsPresenter.guns(@mission.team.agents) %></span>
  <br>

  <hr>

  <!-- MUNITIONS DATA-->
  <h2><%= t('.munitions') %></h2>
  <strong><%= t('.description') %></strong>
  <span><%= MissionsPresenter.munitions(@mission.team.agents) %></span>
  <br>

  <hr>

  <!-- TACKLES DATA-->
  <h2><%= t('.tackles') %></h2>
  <strong><%= t('.waistcoats') %></strong>
  <span><%= MissionsPresenter.waistcoats(@mission.team.agents) %></span>
  <br>
  <strong><%= t('.radios') %></strong>
  <span><%= MissionsPresenter.radios(@mission.team.agents) %></span>
  <br>

  <hr>

  <!-- VEHICLES DATA-->
  <h2><%= t('.vehicles') %></h2>
  <strong><%= t('.description') %></strong>
  <span><%= MissionsPresenter.vehicles(@mission.team) %></span>

  <%= render '/partials/footer' %>
